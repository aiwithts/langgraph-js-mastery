import type { GraphModule } from "../../types";
import { config } from "./config";
import { createGraph } from "./graph";

export const graph18HumanInTheLoop: GraphModule = {
	config,
	createGraph,
};
