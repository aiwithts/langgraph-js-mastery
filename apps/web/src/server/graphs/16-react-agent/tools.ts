import type { StructuredTool } from "@langchain/core/tools";

// TODO (Lesson 17, Step 2): Define searchTool, calculatorTool, and timeTool using tool()
// and export them as handCodedTools

export const handCodedTools: StructuredTool[] = [];
