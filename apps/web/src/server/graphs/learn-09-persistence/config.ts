import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "my-memory-tester",
	name: "My Memory Tester",
	description: "A graph that tests conversation persistence and fact extraction.",
};
