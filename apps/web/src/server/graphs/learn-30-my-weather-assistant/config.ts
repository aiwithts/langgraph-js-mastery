import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "learn-30-my-weather-assistant",
	name: "My Weather Assistant",
	description: "Dispatches display intents to render rich weather UI components.",
};
