import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "learn-02-my-processor",
	name: "My Multi-Step Processor",
	description: "Processes input through 3 steps, demonstrating state reducers.",
};
