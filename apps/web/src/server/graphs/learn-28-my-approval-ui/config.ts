import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "learn-28-my-approval-ui",
	name: "My Approval UI",
	description: "Combines interrupt() for human-in-the-loop with rich UI components for confirmations.",
};
