import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "my-context-manager",
	name: "My Context Manager",
	description: "A chatbot that manages long conversations intelligently using summarization.",
};
