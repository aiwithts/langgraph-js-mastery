import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "learn-20-routing",
	name: "Service Router",
	description: "Customer service router with confidence-based triage that classifies requests and routes to specialist handlers.",
};
