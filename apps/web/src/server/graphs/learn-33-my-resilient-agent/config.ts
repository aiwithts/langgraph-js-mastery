import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "learn-33-my-resilient-agent",
	name: "My Resilient Agent",
	description: "An agent with retry logic, fallback handlers, and circuit breaker patterns.",
};
