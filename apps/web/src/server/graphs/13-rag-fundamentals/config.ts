import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "13-rag-fundamentals",
	name: "Knowledge-Augmented RAG Assistant",
	description: "Retrieves relevant documents from a vector store, then generates a grounded answer with source citations.",
};
