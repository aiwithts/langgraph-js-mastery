import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "learn-22-my-parallel-processor",
	name: "My Parallel Processor",
	description: "Fan-out/fan-in processing using the Send API for map-reduce patterns.",
};
