import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "learn-18-my-approval-flow",
	name: "My Approval Flow",
	description: "A human-in-the-loop workflow that pauses for human approval before executing actions.",
};
