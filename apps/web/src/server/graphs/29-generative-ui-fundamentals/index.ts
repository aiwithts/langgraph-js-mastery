import type { GraphModule } from "../../types";
import { config } from "./config";
import { createGraph } from "./graph";

export const graph29GenerativeUiFundamentals: GraphModule = {
	config,
	createGraph,
};
