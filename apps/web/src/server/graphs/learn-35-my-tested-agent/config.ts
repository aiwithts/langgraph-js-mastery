import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "learn-35-my-tested-agent",
	name: "My Tested Agent",
	description: "An agent built with testability in mind: dependency injection, mock LLM, unit tests.",
};
