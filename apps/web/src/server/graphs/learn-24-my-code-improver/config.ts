import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "learn-24-my-code-improver",
	name: "My Code Improver",
	description: "An evaluator-optimizer loop: generate → evaluate → refine until quality threshold.",
};
