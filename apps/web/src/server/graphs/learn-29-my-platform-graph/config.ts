import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "learn-29-my-platform-graph",
	name: "My Platform Graph",
	description: "Connects to a deployed LangGraph Platform graph via RemoteGraph.",
};
