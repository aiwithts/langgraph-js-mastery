import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "05-few-shot-prompting",
	name: "Few-Shot Formatting Assistant",
	description: "Demonstrates few-shot prompting with structured examples to enforce consistent response formatting.",
	endpoint: "/api/stream",
};
