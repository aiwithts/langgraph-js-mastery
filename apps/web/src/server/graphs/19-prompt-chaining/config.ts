import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "19-prompt-chaining",
	name: "Draft Critique Refine Pipeline",
	description: "Chains three LLM steps — draft, critique, and refine — to produce polished content from a single topic.",
};
