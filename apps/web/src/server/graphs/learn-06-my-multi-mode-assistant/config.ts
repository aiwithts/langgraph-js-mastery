import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "my-multi-mode-assistant",
	name: "My Multi-Mode Assistant",
	description: "An assistant that switches between code, concept, and debug modes.",
};
