import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "learn-21-my-composed-system",
	name: "My Composed System",
	description: "A parent graph that orchestrates subgraphs, mapping state between graph boundaries.",
};
