import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "learn-26-my-ui-assistant",
	name: "My UI Assistant",
	description: "A generative UI assistant that dispatches custom UI components via config.writer.",
};
