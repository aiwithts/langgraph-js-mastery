import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "42-application-structure",
	name: "Application Structure & Deployment",
	description:
		"Configures a LangGraph project for deployment via langgraph.json and explores production deployment options.",
	endpoint: "/api/stream-thread",
	persistent: true,
};
