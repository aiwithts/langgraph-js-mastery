import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "learn-19-my-pipeline",
	name: "My Pipeline",
	description: "A prompt chaining pipeline: draft → critique → refine for high-quality output.",
};
