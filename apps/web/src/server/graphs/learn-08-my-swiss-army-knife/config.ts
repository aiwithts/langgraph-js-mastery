import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "learn-08-swiss-army-knife",
	name: "Learn 08: Swiss Army Knife",
	description: "Multi-mode assistant that classifies intent, routes by confidence, and responds with specialized handlers.",
};
