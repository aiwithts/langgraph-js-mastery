import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "learn-36-my-production-agent",
	name: "My Production Agent",
	description: "A production-ready agent with model routing, environment validation, and structured logging.",
};
