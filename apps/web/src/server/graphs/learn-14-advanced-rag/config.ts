import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "my-hybrid-rag-assistant",
	name: "My Hybrid RAG Assistant",
	description: "Advanced RAG with hybrid retrieval: keyword (BM25) + semantic + multi-query.",
};
