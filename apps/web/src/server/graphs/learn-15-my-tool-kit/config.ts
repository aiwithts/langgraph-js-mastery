import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "learn-15-my-tool-kit",
	name: "My Tool Kit",
	description: "An agent equipped with custom tools using the tool() decorator and Zod schemas.",
};
