import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "my-formatting-assistant",
	name: "My Formatting Assistant",
	description: "An assistant that uses few-shot prompting for consistent formatting.",
};
