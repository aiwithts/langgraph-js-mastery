import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "learn-34-my-observable-agent",
	name: "My Observable Agent",
	description: "An agent instrumented with LangSmith tracing and custom performance metrics.",
};
