import type { GraphInfo } from "@/types";

export const config: GraphInfo = {
	id: "learn-32-my-safe-agent",
	name: "My Safe Agent",
	description: "An agent with input validation, output filtering, and PII detection guardrails.",
};
